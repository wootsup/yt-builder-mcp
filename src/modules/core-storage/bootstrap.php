<?php
/**
 * core-storage module bootstrap.
 *
 * Reserved namespace for the wp_option('yootheme') reader/writer that
 * Wave 2 (read-ops) and Wave 3 (write-ops) will populate, along with the
 * ETag accounting documented in the spike-outcomes.
 *
 * Wave 1 intentionally ships no services here; the file exists so the
 * YOOtheme loader's brace-glob resolves cleanly.
 *
 * @package WootsUp\BuilderMcp\Storage
 */

declare(strict_types=1);

return [];
