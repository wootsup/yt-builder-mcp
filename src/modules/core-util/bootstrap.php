<?php
/**
 * core-util module bootstrap.
 *
 * Reserved namespace for cross-cutting helpers (Logger, Validator, Path).
 * Wave 1 intentionally ships no services here — the file exists so that
 * `app()->load(.../{core-util,…}/bootstrap.php)` resolves cleanly. Future
 * Wave 2+ work will populate this module.
 *
 * @package WootsUp\BuilderMcp\Util
 */

declare(strict_types=1);

return [];
